{
  "name": "violet-motion",
  "version": "1.0.0",
  "description": "Violet Motion landing page + admin server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "bot": "node bot.js",
    "dev": "nodemon server.js",
    "dev:bot": "nodemon bot.js",
    "check": "node --check server.js && node --check bot.js && node --check crm-integration.js && node --check nova-poshta.js && node --check monobank.js && node --check np-order-merge.js && node --check telegram-update-queue.js",
    "test": "node --test",
    "verify": "npm run check && npm test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-telegram-bot-api": "^1.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
